This file is created in firmaware branch
This is for new commit
this is after merging firmware into dev branch

if (this is firmware)
{
    this is firmware branch 
}


int getFirmwareVersion(void)
{
    int i = 0;
    i++;
    for (int j = k; i < k; j++)
    {
        //this is never ending looping
    }
}
